import type { Color } from "~/types";

export default function Path({
  x,
  y,
  stroke,
  fill,
  opacity,
  points,
}: {
  x: number;
  y: number;
  stroke: Color;
  fill: Color;
  opacity: number;
  points: number[][];
}) {
    return <path></path>
}
